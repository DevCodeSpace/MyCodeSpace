import 'package:flutter/material.dart';

class AppColors {
  // --- Primary Brand Colors ---
  static const Color primary = Color(0xFF00B4A2);

  // --- Background & Surface Colors (Light Mode) ---
  static const Color backgroundLight = Colors.white;
  static const Color surfaceLight = Color(0xFFE0FAF4);
  static const Color textLight = Color(0xFFFCFFFF);

  // --- Background & Surface Colors (Dark Mode) ---
  static const Color backgroundDark = Colors.black;
  static const Color surfaceDark = Color(0xFF1E2928);
  static const Color textDark = Color(0xFF1B1B1B);

  // --- Status / Semantic Colors ---
  static const Color success = Color(0xFF2E7D32); // Green
  static const Color error = Color(0xFFD32F2F); // Red
  static const Color warning = Color(0xFFF57C00); // Orange
  static const Color info = Color(0xFF0288D1); // Blue
}
